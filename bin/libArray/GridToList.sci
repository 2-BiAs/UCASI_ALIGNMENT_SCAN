function mOutput = GridToList(mX, mY)
    //Takes a Grid Generated by ndgrid, or such, and converts to a list
    
    [iN, iM] = size(mX);
    
    for i = 1:1:iN
        for j = 1:1:iM
            mOutput((i - 1) * iM + j, 1:2) = [mX(i, j) mY(i, j)];
        end
    end
      
endfunction
