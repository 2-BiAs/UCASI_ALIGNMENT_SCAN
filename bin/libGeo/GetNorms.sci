function mOutput = GetNorms(mPoints, spParams)
    //Generates an array of normal vectors [i, j, k]
    //Given an array of [u, v], points and surface parameter 
    //structure defining general polynomial freeform surface
    
    
endfunction
